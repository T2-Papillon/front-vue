version: '3'

services:
    nginx_server:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - ./dist:/usr/share/nginx/html
            - ./nginx/conf/:/etc/nginx/conf.d
        ports:
            - '5174:80'
        restart: always
